package custom.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import custom.model.VLaptopReturned;

public interface VLaptopReturnedRepo extends JpaRepository<VLaptopReturned, Long> {
}
